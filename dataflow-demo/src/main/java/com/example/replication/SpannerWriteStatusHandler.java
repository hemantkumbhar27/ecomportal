package com.example.replication;

import com.example.EcomPipelineOptions;
import org.apache.beam.sdk.io.gcp.spanner.SpannerConfig;
import org.apache.beam.sdk.io.gcp.spanner.SpannerWriteResult;

public class SpannerWriteStatusHandler {


    public static void createFailedStatus(SpannerWriteResult writeResult, EcomPipelineOptions options, SpannerConfig spannerConfig) {

    }
}
